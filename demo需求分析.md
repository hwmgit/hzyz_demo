# 教培收费系统 Demo 需求分析

执行日期：2026-06-24  
执行者：Codex

## 1. 真实业务目标

本 Demo 用于向教培客户演示“学校下发缴费链接/二维码后，家长在线完成身份核验、查看应缴项目并完成缴费；管理后台同步维护学生、配置收费项目、监控缴费状态”的完整业务闭环。

当前目标不是建设真实生产系统，而是用 H5 假数据交互帮助客户理解产品流程、页面结构和核心能力。

## 2. 角色与使用场景

### 学生和家长端

- 家长通过学校分发的缴费 URL 或二维码进入 H5 页面。
- 页面要求输入学生姓名和身份证号，用于关联后台已导入的学生缴费信息。
- 核验通过后，展示该学生的待缴项目、金额、缴费周期和汇总金额。
- 家长确认后进入聚合支付演示流程，点击后模拟付款成功。
- 支付成功后，系统展示缴费回执，并同步更新后台状态。

### 管理后台

- 管理员导入或新增学生信息，字段包含学生姓名、身份证号、班级、家长手机号等。
- 管理员配置收费项目、金额和适用周期，例如代管费、住宿费、教材费。
- 管理员查看应收、已收、未收、缴费完成率等统计。
- 管理员按学生查看缴费状态，区分已支付和待支付。
- 管理员查看部署 URL 说明，部署后可自行用该 URL 生成缴费二维码并分发。

## 3. 本次 Demo 范围

- 纯前端静态 H5 Demo，不接后端。
- 家长端和管理后台拆分为两个页面：`index.html` 仅作为家长扫码进入的缴费页，`admin.html` 作为管理后台页面。
- 中文单语界面，不做中英双语切换。
- 不做 Demo 内扫码模拟入口；部署后由用户按 `index.html` 的 URL 自行生成二维码，客户扫码只进入家长端页面，不展示管理后台。
- 不接真实聚合支付通道，只做聚合支付演示弹窗和付款成功状态。
- 不做真实 Excel 解析后端；后台提供“导入示例数据”和“新增学生”演示。
- 家长端身份核验必须和后台学生数据使用同一份前端状态；两页通过同源浏览器存储共享演示数据，体现导入数据与扫码录入数据的一致性校验。

## 4. 核心流程

### 4.1 管理后台准备数据

1. 管理员进入后台。
2. 点击“导入示例学生”，系统向统一学生数据池追加学生。
3. 或通过新增学生表单录入姓名、身份证号、班级、手机号。
4. 配置收费项目金额，系统自动计算每名学生的应缴金额。
5. 后台展示缴费状态与汇总金额。

### 4.2 家长端缴费

1. 家长通过部署后的 H5 URL 进入页面。
2. 输入学生姓名和身份证号。
3. 系统从后台同一份学生数据池中查找：
   - 姓名不存在：提示未找到该学生缴费信息。
   - 姓名存在但身份证号不一致：提示身份信息不匹配。
   - 姓名和身份证号一致：核验通过。
4. 核验通过后展示该学生待缴费用。
5. 点击“确认并支付”，展示聚合支付演示确认。
6. 点击“模拟支付成功”，更新该学生缴费状态为已支付。
7. 后台统计、状态表同步刷新。

## 5. 数据字段

### 学生

- id：学生唯一标识
- name：学生姓名
- idNo：身份证号
- className：班级
- guardianPhone：家长手机号
- importedAt：导入或新增时间

### 收费项目

- id：项目唯一标识
- name：项目名称
- cycle：适用周期
- amount：金额
- enabled：是否启用

### 缴费状态

- studentId：学生 ID
- status：paid 或 pending
- paidAt：支付时间
- channel：支付渠道演示名称

## 6. 页面信息架构

### 家长端

- 学校收费说明
- 身份核验表单
- 核验结果提示
- 待缴项目明细
- 聚合支付演示
- 支付成功回执

### 管理后台

- 经营统计卡片
- 学生数据管理
- 收费项目配置
- 缴费状态监控
- 缴费 URL 与二维码生成说明

## 7. 验收标准

- 家长端必须能输入姓名和身份证号进行核验。
- 后台新增或导入的学生必须能被家长端核验命中。
- 姓名和身份证号不一致时必须失败，不得只按姓名通过。
- 核验通过后必须展示该学生待缴项目和合计金额。
- 支付成功后后台该学生状态变为已支付，统计金额同步变化。
- 扫码落地页不得展示管理后台模块或后台入口信息。
- 界面只保留中文，不出现语言切换。
- 页面不包含扫码模拟入口。
- 本地不需要 Chrome 截图验证，以静态脚本检查和手工打开页面可用性为准。

## 8. 后续真实系统需要准备的资源

- 客户指定聚合支付通道资料、商户号、回调地址、签名规则。
- 真实学生数据导入模板和字段校验规则。
- 身份证号等敏感信息的存储、脱敏、权限和审计规则。
- 学校、班级、收费周期、订单号、退款、对账等真实业务规则。
- 部署域名和正式缴费二维码生成方式。
