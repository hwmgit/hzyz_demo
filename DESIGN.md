# 软剑熊案例展示系统设计规范

生成日期：2026-06-09
执行者：Codex AI

## 1. 设计定位

本项目采用“克制的企业数据看板入口页 + 移动端客户展示端 + 网页管理后台”三段式风格。设计参考 MUJI 数据看板基线，并结合 `D:\demo\ysb丽水文旅` 的小程序交互骨架，以及软剑熊客户资料中的品牌红、收银设备和门店现场素材，形成适合正式系统汇报的高可信界面。

项目根目录 `DESIGN.md` 是唯一设计约束源。

## 2. 颜色 Token

- 页面背景：`#f3ede4`、`#ece3d5`
- 主文字：`#2c241c`
- 次级文字：`#776b5e`
- 品牌强调红：`#9f1f24`
- 辅助棕色：`#8b6944`
- 成功色：`#4c7b62`
- 警告色：`#cc8b35`
- 危险色：`#b85143`
- 面板背景：`rgba(255, 251, 245, 0.88)`
- 边框：`rgba(91, 69, 49, 0.14)`

## 3. 字体与排版

- 中文优先：`Microsoft YaHei`、`PingFang SC`、系统无衬线。
- 英文优先：`Segoe UI`、系统无衬线。
- 不使用负字距，不使用随视口宽度变化的字体大小。
- 后台表格、标签、状态文案保持紧凑清晰。
- 小程序预览区域可使用更强的标题层级，但不能遮挡图片或按钮。

## 4. 布局规范

- 首页入口页采用桌面卡片式双入口布局。
- 小程序端采用 414px 手机壳画布，顶部标题栏 + 滚动内容区 + 底部导航。
- 小程序端首页必须包含主视觉轮播、快捷入口、推荐案例或讲解路径。
- 管理后台作为独立页面承接，需具备案例列表、编辑区、资料库和上传入口。

## 5. 组件规范

- 卡片圆角：12px 到 20px，重复内容卡片不超过 16px。
- 按钮高度：36px 到 42px。
- 输入框高度：38px。
- 标签使用低饱和半透明底色。
- 表格头部使用浅米色底和细边框。
- 状态反馈使用固定 Toast，不使用浏览器 alert。
- 弹窗用于案例详情和编辑预览，背景遮罩必须低透明度。

## 6. 图片使用

- 真实客户素材优先用于案例封面、门店图集、素材库和服务工单。
- 图片必须设置稳定比例，避免加载后造成布局跳动。
- 图片需要 `object-fit: cover`，不拉伸变形。
- 对现场照片可加轻微暗角或低透明遮罩增强文字可读性，但不能过度模糊。

## 7. 交互规范

- 首页入口按钮必须明确跳转目标。
- 小程序端主按钮必须可点击并有页面切换或状态反馈。
- 小程序端首页轮播必须支持触控左右切换。
- 案例详情必须是完整页面，不做空白页。
- 表单提交：展示成功反馈，不发起网络请求。

## 8. 响应式要求

- 首页桌面端为双栏入口布局，移动端自动改单列。
- 小程序手机壳宽度默认 414px，移动端不得超过视口宽度。
- 底部导航和快捷入口文字不得溢出。

## 9. 禁止事项

- 禁止接入后端或真实提交数据。
- 禁止依赖外部 CDN。
- 禁止使用大面积紫蓝渐变、纯深色科技风或泛化模板风。
- 禁止在业务区堆叠装饰卡片。
- 禁止内联样式覆盖核心组件规范。
