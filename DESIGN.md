# v5 官网设计规范

## 1. 设计目标

v5 当前以 v2 明亮 AI 数智化官网为基底创建。初始目标是保留 v2 的明亮清爽风格和多页面结构，后续在此基础上继续吸收参考模板中的内容组织、入场动画、案例展示和交互细节。

## 2. 视觉资产

本版本初始沿用 v2 的本地 PNG 位图资产，不依赖远程图片。

- 首页首屏背景：不使用图片，采用暖白色到浅蓝色的多层 CSS 渐变循环。
- 首页主模块背景：服务矩阵、交付流程、联系表单之间使用连续的暖白到浅蓝渐变，保持上下模块自然过渡。
- `assets/module-back-compressed.jpg`：由根目录 `back.png` 压缩裁切而来，用于 AI 顾问、AI 软件和商业运营模块图片。
- `assets/out_cn.jpeg`：用于首页四大核心业务中的出海业务模块图片。
- `assets/youzan-showcase/`：临时用于复刻有赞首页右侧卡片的本地图片素材，正式上线前替换为自有或已授权素材。

## 3. 色彩

- 背景：`#f6fbff`。
- 主文本：`#09213f`。
- 辅助文本：`#55708e`。
- 主蓝：`#137cf7`。
- 数智青：`#18c5d9`。
- 增长绿：`#26d4a4`。
- 边框：`#d7e9f7`。

## 4. 组件规则

- 卡片使用 20px-24px 圆角，保持明亮、轻盈、现代。
- 按钮使用胶囊形态，主按钮采用蓝青渐变。
- 四大核心业务模块采用图片铺底、文字浮在图片上方的卡片结构。
- 首屏文案叠加在暖白到浅蓝的柔和循环渐变上，保证中文标题对比度。
- 首屏左侧使用有赞式业务标签 + 标题纵向自动切换，标签固定为“AI转型 / 软件定制 / 实体商业 / 商业出海”；标签为白底胶囊容器，激活项为深色底白字。
- 左侧大标题与说明文案必须同步切换，说明文案聚焦对应业务的可落地价值，不写泛泛口号。
- 首屏左侧大标题必须完整可见且清晰，不能被右侧卡片墙挤出视口；标题使用清晰重字重、无阴影、无负字距。
- 首屏右侧允许使用三列纵向滚动业务卡片墙，桌面端按 630px 宽、3 条 194px 轨道、24px 列间距呈现，卡片高度约 260px、圆角 16px，视觉上参考有赞首页右侧卡片流。
- 卡片必须分为独立图片卡 `.showcase-card--image` 和独立白底文字卡 `.showcase-card--text`，两者交替滚动；禁止把文字压在图片卡内部。文字卡高度保持短卡样式，内容只允许两行：一行标题、一行简介。
- 文字卡标题必须为深色清晰文字，图片卡的白色标题样式只能作用于 `.showcase-card--image`，不得使用会覆盖全部卡片的通用 `strong` 规则。
- 图片卡通过 `.showcase-visual` 背景图接入，当前使用本地下载的有赞参考图；滚动由 `script.js` 持续更新轨道 `transform`，当前速度为 `data-speed="0.03"`。
- 右侧轨道滚动由 JS 每帧驱动，不能再叠加 CSS `transform` 过渡；卡片 hover 只能作用于单张卡片自身，不能作用于整条轨道。
- AI 顾问页使用咨询服务叙事：先讲 AI 转型为什么卡住，再给出诊断方法、交付物和试点路线图，避免写成纯软件开发服务页。
- 资料驱动内容版本采用“管理方法论 + 可落地交付物”的表达方式：先说业务痛点和方法，再给出流程、平台、工具或资源，不写泛泛的科技口号。
- 各独立页内容应与资料对应：AI 顾问页突出精益 + 流程双驱和五步闭环；AI 软件页突出意图编程和软件 3.0；出海页突出双基融合和本地化落地；商业运营页突出实体商业数智运营平台；关于页区分杭州盈泽主体和瑞维体系经验。

## 5. 待替换项

- 公司真实名称。
- Logo / 商标。
- 备案号。
- 电话、邮箱、地址。
- 真实案例与客户信息。

