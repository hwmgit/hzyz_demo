# 教培收费系统 Demo 设计规范

执行日期：2026-06-24  
执行者：Codex

## 1. 设计定位

本项目是教培收费系统静态 H5 Demo，界面服务于售前演示和客户流程确认。设计语言采用克制、清晰、可信的浅色数据看板风格，兼顾家长端移动缴费体验和后台管理效率。

项目根目录 `DESIGN.md` 是唯一设计约束源。

## 2. 技术与交互约束

- 中文单语界面，不提供语言切换。
- 不在 Demo 内提供扫码模拟入口；部署后由用户按 `index.html` 页面 URL 生成二维码。
- `index.html` 是家长端扫码落地页，只展示身份核验、缴费明细和支付演示，不展示管理后台。
- `admin.html` 是管理后台页，用于学生导入、收费配置、状态监控和缴费 URL 分发说明。
- 不接真实后端，演示数据来自前端状态。
- 后台学生导入/新增和家长端身份核验必须使用同一份学生数据池；拆分页面后通过同源 `localStorage` 保持演示数据一致。
- 不使用 Chrome 截图验证，采用静态脚本和页面文件检查验证。
- 预埋 Umami 统计能力，但仅在生产域名和 websiteId 都配置时启用。

## 3. 色彩规范

基于 MUJI 数据看板风格收敛为教育收费场景：

- 页面背景：`#f5efe6`
- 次背景：`#ece3d5`
- 主文字：`#2c241c`
- 次文字：`#776b5e`
- 主操作：`#8b6944`
- 成功：`#4c7b62`
- 警告：`#cc8b35`
- 危险：`#b85143`
- 卡片背景：`rgba(255, 251, 245, 0.88)`
- 边框：`rgba(124, 98, 67, 0.18)`

避免大面积深色、紫蓝渐变或过度装饰。

## 4. 布局规范

### 整体

- 首屏使用产品标题、关键流程说明和主要状态摘要。
- 家长端和管理后台必须拆分页面，禁止在扫码落地页同时展示后台。
- 家长端页面最大宽度控制在 720px 内，优先移动端缴费体验。
- 管理后台页面最大宽度控制在 1320px 内，保持数据管理信息密度。

### 家长端

- 偏移动 H5 卡片式布局。
- 身份核验表单放在家长端第一操作位。
- 核验通过后展示缴费明细和支付按钮。
- 支付成功回执使用明显但克制的成功状态。

### 管理后台

- 使用 KPI 卡片、表格、配置表单和状态标签。
- 表格信息密度适中，适合演示“已付/待付”和金额汇总。
- 导入学生、收费配置、状态监控按业务顺序排列。

## 5. 组件规范

- 按钮高度不低于 40px，主要按钮使用主操作色。
- 输入框高度不低于 42px，边框和 focus 状态保持统一。
- 状态标签使用低饱和背景，不使用强闪烁动画。
- 表格头部背景轻微区分，文字保持可读。
- 弹窗用于聚合支付演示确认，避免跳出当前页面。

## 6. 文案规范

- 使用正式、清晰、业务化中文。
- 不使用“扫码模拟入口”“中英切换”等与当前验收无关的演示词。
- 支付相关文案明确标注“演示”，避免误解为真实支付。
- 身份证号在后台展示时尽量脱敏，完整号码仅用于演示校验说明和输入。

## 7. 状态与反馈

- 核验成功：绿色状态提示。
- 学生不存在：提示未找到该学生缴费信息。
- 身份证不匹配：提示身份信息不匹配。
- 支付成功：更新家长端回执和后台状态。
- 导入成功：提示导入学生数量并刷新列表。

## 8. 响应式要求

- 360px 宽度下按钮、输入框和表格不得出现文字重叠。
- 后台表格在窄屏下允许横向滚动。
- 家长端缴费卡片在移动端优先展示。

## 9. 验证方式

- 使用 `node scripts/validate-demo.mjs` 做静态验证。
- 验证重点：关键文件存在、中文单语、无扫码模拟入口、家长端与后台拆分、扫码页无后台模块、统一学生数据池、身份核验、支付同步、生产统计门禁。
- 本任务不要求使用 Chrome 截图验证。
