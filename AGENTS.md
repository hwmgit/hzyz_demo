# ysb_案例展示 项目说明

## 项目结构

- `index.html`：系统首页入口页，只负责跳转到小程序端或管理端。
- `styles.css`：首页入口页和管理端入口页样式。
- `mini.html`：小程序展示端入口。
- `mini.css`：小程序展示端样式。
- `mini.js`：小程序展示端假数据、页面状态切换和交互逻辑。
- `admin.html`：管理后台入口。
- `admin.css`：管理后台样式。
- `admin.js`：管理后台案例筛选、查看、编辑和资料上传预览逻辑。
- `demo需求分析.md`：Demo 唯一需求输入源，后续修改功能前优先读取。
- `DESIGN.md`：项目唯一设计约束源，后续 UI 修改必须遵循。
- `资料/`：客户提供的图片和企业画册素材。
- `.codex-log/`：Codex 操作留痕。
- `.codex-tmp/`：临时文件目录，不作为正式交付内容。

## 运行方式

可直接双击打开 `index.html`，也可在项目根目录启动本地静态服务：

```powershell
python -m http.server 4173 --bind 127.0.0.1
```

然后访问：

```text
http://127.0.0.1:4173/
```

## 重要约定

- 当前为静态交付阶段，不接后端、不建数据库、不发送真实表单。
- 页面内数据均为假数据，但业务语义来自客户资料。
- 首页只负责入口，不再把小程序端和管理端堆叠在同一页。
- 小程序端参考 `D:\demo\ysb丽水文旅` 的手机端交互结构，但内容必须保持软剑熊案例展示语义。
- 图片直接引用 `资料/` 下中文文件名。
- 页面预埋 Umami 统计接入结构时，默认保持本地禁用。
- 修改 UI 前先读 `DESIGN.md`；修改需求前先读 `demo需求分析.md`。

## 已知注意事项

- `软剑熊-企业画册-0806.pdf` 是扫描型画册，无法直接提取文字；当前需求分析基于视觉抽查和用户补充。
- 若后续要部署到服务器，需确保 `资料/` 目录与 `index.html`、`mini.html`、`admin.html` 一起发布。
- 管理后台当前已支持案例查看、筛选、编辑、封面切换和本地资料上传预览，后续继续补齐更细的状态流转与权限设计。
- 若后续转正式系统，建议拆分为微信小程序展示端、Vue/React 管理端、后端 API 和数据库，不要在当前静态 Demo 上继续堆生产逻辑。

## 后续正式化建议

- 小程序端：微信小程序原生或 uni-app。
- 管理端：Vue 3 + Vite + Element Plus。
- 后端：Node.js/NestJS 或 Python/FastAPI。
- 数据库：MySQL，案例、分类、素材、线索、工单分表管理。
