# hzyz_demo 项目 AGENTS.md

## 项目结构

- `index.html`：家长端缴费页，是部署后用于生成二维码的扫码落地页，不展示管理后台。
- `admin.html`：收费管理后台，包含学生导入、收费配置、状态监控和缴费 URL 分发说明。
- `demo需求分析.md`：本 Demo 的需求输入源。
- `DESIGN.md`：项目唯一设计约束源。
- `scripts/validate-demo.mjs`：本地静态验证脚本。
- `.codex-log/`：本地操作留痕目录。

## 技术决策

- 当前项目是纯前端静态 Demo，不接后端、不接真实数据库、不接真实支付。
- 页面只做中文单语，不做中英双语切换。
- Demo 内不做扫码模拟入口；部署后由用户按 `index.html` 的 URL 自行生成二维码。
- 管理后台导入或新增学生后，必须写入统一学生数据池；家长端身份核验只能从同一数据池匹配。
- 拆分页面后，`admin.html` 和 `index.html` 通过同源 `localStorage` 共享演示数据。
- 聚合支付仅做演示弹窗和状态同步，不发起真实扣款。
- Umami 统计能力只做生产环境预埋，必须同时配置生产域名、websiteId 和脚本地址后才允许发送数据。

## 已知坑点与注意事项

- 不要把家长端核验数据单独写死，否则会破坏“后台导入后前端可核验”的核心演示价值。
- 不要把管理后台模块放回 `index.html`，否则客户扫码会看到后台。
- 身份核验必须同时校验学生姓名和身份证号；只按姓名通过是不符合需求的。
- 后台展示身份证号时尽量脱敏；完整号码只用于演示输入和校验。
- 已支付状态需要同步影响后台 KPI、学生状态表和家长端回执。
- 本任务明确不使用 Chrome 截图验证，验收以静态脚本和页面可打开为准。

## 验证命令

```powershell
node scripts/validate-demo.mjs
```

## 后续真实系统扩展方向

- 替换前端内存数据为后端接口和数据库。
- 对接客户指定聚合支付通道、回调通知、订单号、退款和对账。
- 增加真实 Excel 模板解析、导入校验和错误行反馈。
- 增加登录权限、学校/班级维度、敏感字段脱敏和审计日志。
